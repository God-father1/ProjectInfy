# Natural Language Interface for Graph Databases

## Repository contains the Internship Project provided during the SP Internship Duration.

To run the rasa model use - `rasa run -m models --enable-api --cors “*” --debug`

